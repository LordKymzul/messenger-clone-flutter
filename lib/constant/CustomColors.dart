import 'dart:ui';

import 'package:flutter/material.dart';

class CustomColors {
  static const primaryColor = Color(0xff060720);
  static const secondaryColor = Color(0xffffffff);
  static final loadColors = Colors.grey.shade300;
  static const messengerColor = Color(0xff00B2FF);
}
